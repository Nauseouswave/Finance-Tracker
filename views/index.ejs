<!DOCTYPE html>
<html>
<head>
    <title>Finance Tracker</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <style>
        body {
            background-color: #333;
            color: #fff;
        }
        .navbar {
            background-color: #444;
        }
        .jumbotron {
            background-color: #747474 !important;
        }
        .btn-primary {
            background-color: #008000;
            border-color: #008000;
        }
        .btn-secondary {
            background-color: #006400;
            border-color: #006400;
        }
        .scroll-animate {
            opacity: 0;
            transition: opacity 2s;
        }
        .animate {
            animation: slidein 2s forwards;
        }
        @keyframes slidein {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <%- include('navbar.ejs', {currentPage: currentPage}) %>
    <div class="jumbotron text-center">
        <h1 class="display-4">Welcome to Finance Tracker!</h1>
        <p class="lead">Track your expenses and manage your finances with ease.</p>
        <hr class="my-4">
        <p>Start by logging in or registering a new account.</p>
        <a class="btn btn-primary btn-lg" href="/login" role="button">Login</a>
        <a class="btn btn-secondary btn-lg" href="/register" role="button">Register</a>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-md-6 scroll-animate">
                <img src="https://source.unsplash.com/600x400/?cash" class="img-fluid" alt="Cash Image">
            </div>
            <div class="col-md-6 scroll-animate">
                <img src="https://source.unsplash.com/600x400/?dashboard" class="img-fluid" alt="Dashboard Image">
            </div>
        </div>
    </div>
    <script>
        window.addEventListener('load', (event) => {
            let elements = document.querySelectorAll('.scroll-animate');
            elements.forEach((element) => {
                element.classList.add('animate');
            });
        });
    </script>
</body>
</html>